---
# Tracked example inventory. Put real hosts in inventory/hosts.local.yml.
#
# Example:
# all:
#   children:
#     vps:
#       hosts:
#         my-vps:
#           ansible_host: 203.0.113.10
#           ansible_user: root
#           ansible_become: false
all:
  children:
    vps:
      hosts: {}
